/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   args.c                                             :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: albaud <albaud@student.42.fr>              +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2022/12/07 13:21:16 by albaud            #+#    #+#             */
/*   Updated: 2022/12/07 17:00:47 by albaud           ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include "header.h"

int	is_the_end(t_slink *link)
{
	return (link->type == PIPE || link->type == AND
		|| link->type == OR);
}

int	is_redirection(t_slink *link)
{
	return (link->type == LLEFT || link->type == RRIGHT
		|| link->type == LEFT || link->type == RIGHT);
}

int	arg_num(t_slink *link)
{
	int	res;

	res = 0;
	while (link)
	{
		if (is_the_end(link))
			break ;
		if (!is_redirection(link))
			res++;
		else
			link = link->next;
		link = link->next;
	}
	return (res);
}


t_args	*allok_args(t_slst *args)
{
	t_args	*res;

	res = allok(sizeof(*res), 1, 1);
	res->args = allok(sizeof(char *), arg_num(args->first), 1);
	res->right = allok(sizeof(*res->right), 1, 1);
	res->rright = allok(sizeof(*res->right), 1, 1);
	res->right->first = 0;
	res->right->last = 0;
	res->rright->first = 0;
	res->rright->last = 0;
	res->end = 0;
	return (res);
}

t_args	*slst_to_tab(t_slst *args)
{
	t_args	*res;
	t_slink	*node;
	int		i;

	i = -1;
	node = args->first;
	res = allok_args(args);
	if (pipe(res->fd) == -1)
		ft_garbage_colector(0, 1, 1);
	//TODO treat no args after <>><>><<<
	while (node && !is_the_end(node))
	{
		if (node->type == RIGHT && ++res->end)
		{
			slst_add_back(res->right, node->next->content, 0, 0);
			node = node->next;
		}
		else if (node->type == RRIGHT && ++res->end)
		{
			slst_add_back(res->rright, node->next->content, 0, 0);
			node = node->next;
		}
		else if (node->type == LEFT && ++res->end)
		{
			fd_injection(node->next->content, res->fd[1]);
			node = node->next;
		}
		else if (node->type == LLEFT && ++res->end)
		{
			fd_injection(node->next->content, res->fd[1]);
			node = node->next;
		}
		else
			res->args[++i] = node->content;
		node = node->next;
	}
	if (node)
		res->end = node->type;
	res->args[++i] = 0;
	return (res);
}
